A 52px navy circle pinned to the top-left corner on every standalone page, so there is always one obvious way back.

\`\`\`jsx
<HomeFab href="home.html" />
\`\`\`

It is the only fixed-position element in the system, and it is hidden in print. Left corner even in RTL — that is the trailing corner, out of the reading path.
