The overview tile: a 48px tinted icon square, a heavy title, a count. Six or seven of these are the whole kunikHome home screen.

\`\`\`jsx
<DomainCard icon="heart" label="תרופות ובריאות" count="4 רשומות"
  color="var(--domain-health)" background="var(--domain-health-bg)" />
\`\`\`

Always pair a domain's ink with its own pale surface — the pairs are fixed in \`tokens/colors.css\`.
