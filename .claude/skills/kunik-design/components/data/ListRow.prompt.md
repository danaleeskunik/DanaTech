The workhorse row: upcoming lessons, birthdays, renewals. A card, not a table row — it wraps on a phone.

\`\`\`jsx
<ListRow leading={<DateChip day="שני" date="14.8" />} title="מרים כהן" meta="10:00 · שיעור 4"
  actions={<Button variant="whatsapp" size="sm" icon="whatsapp">תזכורת</Button>} />
\`\`\`

Rows stack with a 9px gap and no dividers — the card border does the separating.
