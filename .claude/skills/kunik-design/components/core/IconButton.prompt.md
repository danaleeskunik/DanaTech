A square (or circular) glyph-only action. Row actions, header controls, and the floating home pill.

\`\`\`jsx
<IconButton icon="edit" size="sm" tone="sunken" title="עריכה" />
<IconButton icon="trash" size="sm" tone="danger" title="מחיקה" />
<IconButton icon="home" size="lg" tone="navy" href="home.html" title="בית" />
\`\`\`

Use \`sm\` only inside a table row. Anywhere a person taps on a phone, use \`md\` (44px) — the source never goes below that outside tables.
