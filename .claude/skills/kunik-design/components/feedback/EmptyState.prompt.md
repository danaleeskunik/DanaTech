One sentence in a quiet box, and it always says what to do next.

\`\`\`jsx
<EmptyState>אין שיעורים קבועים. הוסיפי יום ושעה לתלמיד/ה בכרטיס שלה.</EmptyState>
\`\`\`

Never an illustration, never an icon, never "Nothing here yet." The copy names the exact next action and where it lives.
