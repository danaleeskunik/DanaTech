A practice task. The whole row is the hit area, not just the box.

\`\`\`jsx
<Checkbox checked label="שלחי הודעת שלום לקרוב משפחה" color="var(--blue-500)" />
<Checkbox size="print" label="צלמי 3 תמונות ובדקי שנשמרו בגלריה" />
\`\`\`

Checked rows go grey and strike through on screen; on paper (\`size="print"\`) the box is empty and unstyled so it can be ticked with a pen.
